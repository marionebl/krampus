#!/usr/bin/env node
var port = require('minimist')(process.argv.slice(2))._[0];
var krampus = require('../');

krampus(port, function(err, pid){
  if (err) {
    console.log(err);
    process.exit(1);
  }

  console.log('Successfully killed process with pid', pid);
  process.exit(0);
});
